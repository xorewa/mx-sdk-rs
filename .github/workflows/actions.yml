name: CI

on:
  push:
    branches:
      - NewArc
  pull_request:
    branches:
      - NewArc

permissions:
  checks: write
  pull-requests: write

jobs:
  rust-tests-min:
    name: Rust Tests (rustc 1.85)
    uses: xorewa/mx-sc-actions/.github/workflows/rust-tests.yml@0cea8a2cbfd5f69cf7dfca957868803da69617de
    with:
      rust-toolchain: 1.85
      runs-on: ubuntu-latest

  rust-tests-max:
    name: Rust Tests (rustc 1.96)
    uses: xorewa/mx-sc-actions/.github/workflows/rust-tests.yml@0cea8a2cbfd5f69cf7dfca957868803da69617de
    with:
      rust-toolchain: 1.96
      runs-on: ubuntu-latest

  setup:
    name: Setup Environment
    uses: xorewa/mx-sc-actions/.github/workflows/setup.yml@0cea8a2cbfd5f69cf7dfca957868803da69617de
    with:
      rust-toolchain: 1.96
      runs-on: ubuntu-latest
      rust-target: wasm32v1-none
      path-to-sc-meta: framework/meta
      # Xorewa-only release with reviewed NewArc VM/scenario dependencies and
      # both Linux and Intel-macOS installer artifacts.
      mx-scenario-go-version: v5.1.2-xorewa.1
      wasm-opt-version: 125

  build-contracts:
    name: Build Contracts
    needs: setup
    uses: xorewa/mx-sc-actions/.github/workflows/build-contracts.yml@0cea8a2cbfd5f69cf7dfca957868803da69617de
    with:
      rust-toolchain: 1.96
      runs-on: ubuntu-latest
      rust-target: wasm32v1-none

  wasm-tests:
    name: Wasm Tests
    needs: build-contracts
    uses: xorewa/mx-sc-actions/.github/workflows/wasm-tests.yml@0cea8a2cbfd5f69cf7dfca957868803da69617de
    with:
      rust-toolchain: 1.96
      runs-on: ubuntu-latest

  generate-report:
    name: Generate Report
    needs: build-contracts
    uses: xorewa/mx-sc-actions/.github/workflows/report.yml@0cea8a2cbfd5f69cf7dfca957868803da69617de
    with:
      rust-toolchain: 1.96
      runs-on: ubuntu-latest

  test-coverage:
    name: Test Coverage
    if: false  # Temporarily disabled
    needs: setup
    uses: xorewa/mx-sc-actions/.github/workflows/coverage.yml@0cea8a2cbfd5f69cf7dfca957868803da69617de
    with:
      rust-toolchain: 1.96
      runs-on: ubuntu-latest
      coverage-args: "--ignore-filename-regex='meta/src' --ignore-filename-regex='wasm-adapter' --ignore-filename-regex='benchmarks/' --ignore-filename-regex='tests/' --output ./coverage.md"

  proxy-compare:
    name: Proxy Compare
    needs: setup
    uses: xorewa/mx-sc-actions/.github/workflows/proxy-compare.yml@0cea8a2cbfd5f69cf7dfca957868803da69617de
    with:
      rust-toolchain: 1.96
      runs-on: ubuntu-latest

  clippy-check:
    name: Clippy linter check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: 1.96
          components: clippy
      - uses: giraffate/clippy-action@v1
        env:
          RUSTFLAGS: ""
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          clippy_flags: "--all-targets --all-features"

  formatting:
    name: cargo fmt
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      # Ensure rustfmt is installed and setup problem matcher
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: 1.96
          components: rustfmt
      - name: Rustfmt Check
        run: cargo +1.96 fmt --all -- --check
